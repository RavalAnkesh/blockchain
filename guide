Python — python --version

Pip — pip --version

Node.js — node -v

NPM — npm -v

VS Code — code .

Virtual Environment (Create) — python -m venv venv

Activate venv (Windows) — venv\Scripts\activate

Web3.py — pip install web3

py-solc-x — pip install py-solc-x

Project Folder — mkdir web3_py_simple_storage 
change folde   - cd web3_py_simple_storage

Open Terminal (VS Code) — Ctrl + `


Install ganache - npm install -g ganache-cli

run ganache   :- ganache-cli -d
